export const doesNotExistError = (field: string) => `${field} path doesn't exist in package.json`;
export const mustBeRelativeError = (field: string) => `${field} must use a relative path for value`;
